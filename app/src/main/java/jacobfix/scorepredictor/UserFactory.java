package jacobfix.scorepredictor;

import java.util.Collection;

public class UserFactory {

    private SyncableCache mDetailsCache = new SyncableCache();
    private SyncableCache mPredictionsCache = new SyncableCache();

    public boolean getUserDetails(Collection<String> ids,)
}
